appId: com.example.flutter_app
tags: ["P1", "video-call", "cross-cutting"]
---
# Test: Video Call Feature
# Priority: Medium
# Description: User initiates and participates in a video call.

- runFlow: "maestro/helpers/common_flows/sign_in_patient.yaml"
- # Navigate to video call screen (may be via menu or direct route)
- tapOn:
    id: "menu"
- assertVisible:
    text: "Video Call"
    optional: true
- # Or check if video call is accessible via another route
- # For now, verify video call screen exists if accessible
- assertVisible:
    text: "Video Call"
    optional: true
- # If video call screen is accessible:
- # tapOn: "Start call"
- # assertVisible: "Call controls"
- # tapOn: "End call"
- # assertVisible: "Dashboard"
